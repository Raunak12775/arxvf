$if(doublespacing)$
\usepackage{setspace}
\doublespacing
$endif$
$if(title)$
\title{\LARGE\textbf{\textsf{$title$}}}
$endif$
\def\asep{\\\\\\ } % default: all authors on same column
$if(authorcols)$
\def\asep{\And }
$endif$
\author{${ by-author:_authors.tex()[\asep] }}
\date{$date$}
$if(linenumbers)$
\usepackage{lineno}
\linenumbers
$endif$
